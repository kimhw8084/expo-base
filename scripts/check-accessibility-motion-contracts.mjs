import fs from'node:fs';import path from'node:path';import process from'node:process';const root=process.cwd(),appsRoot=path.join(root,'apps'),problems=[];const forbidden=[[/from\s+['"]react-native-reanimated['"]/,'direct Reanimated usage; use @precision-calm/motion'],[/from\s+['"]expo-haptics['"]/,'direct haptics usage; use @precision-calm/motion'],[/\bAccessibilityInfo\.announceForAccessibility\s*\(/,'direct accessibility announcement; use @precision-calm/accessibility LiveRegion'],[/accessibilityRole\s*=\s*['"]none['"]/,'suppressing semantics requires accessibility-system review']];function walk(dir){if(!fs.existsSync(dir))return;for(const e of fs.readdirSync(dir,{withFileTypes:true})){const f=path.join(dir,e.name);if(e.isDirectory())walk(f);else if(/\.(ts|tsx)$/.test(e.name)){const t=fs.readFileSync(f,'utf8');for(const[p,l]of forbidden)if(p.test(t))problems.push(`${path.relative(root,f)}: ${l}`)}}}if(fs.existsSync(appsRoot))for(const app of fs.readdirSync(appsRoot,{withFileTypes:true}))if(app.isDirectory())walk(path.join(appsRoot,app.name,'app'));if(problems.length){console.error('Accessibility/motion contract violations:\n'+problems.map(x=>`- ${x}`).join('\n'));process.exit(1)}console.log('Accessibility/motion contract check passed.');
